import { NgMyOwnStyleDirective } from './ng-my-own-style.directive';

describe('NgMyOwnStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new NgMyOwnStyleDirective();
    expect(directive).toBeTruthy();
  });
});
